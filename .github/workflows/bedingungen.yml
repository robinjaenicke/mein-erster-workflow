name: Übung1

on:
  push:
  workflow_dispatch:


jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Immer
        run: echo "${{ github.event_name }}"    
      - name: Nur bei Push
        run: echo "${{ github.ref_name }}"            
name: Übung1

on:
  push:
  workflow_dispatch:


jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Eventname
        run: echo "${{ github.event_name }}"    
      - name: Branchname
        run: echo "${{ github.ref_name }}"            
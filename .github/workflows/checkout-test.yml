name: Übung3

on:
  push:
  workflow_dispatch:

jobs:
  Checkout:
    runs-on: ubuntu-latest  
    steps:
      - run: cat hallo.txt
    continue-on-error: true
    
    steps:
      - uses: actions/checkout
      - run: cat hello.txt